# Backend

Базовое окружение для API на Node.js + Fastify.

## Команды

- `npm install`
- `npm run dev`
- `npm start`

## Переменные окружения

Скопируйте `.env.example` в `.env` и при необходимости измените значения.
